# Changelog

## 1.0.2 — 2026-09-18

- 修复 1.0.1 图标初始化表达式错误导致的 `app is not defined` 启动失败。
- 将前端初始化代码展开为可读结构；装饰性图标缺失或加载失败不再阻断队列和打印设置初始化。
- 诊断改为执行真正的 WebView2 HTML/JavaScript 和 Python 桥接启动流程，检查就绪状态、错误弹窗、打印设置和图标加载。
- 前端诊断使用独立隐藏窗口，并阻止任何打印提交，不操作用户当前窗口。
- 保留已有图标、打印逻辑和紧凑布局。

## 1.0.1 — 2026-09-18

- 统一使用根目录 `icon.ico` 作为 EXE、窗口/任务栏和自绘标题栏的应用图标。
- 将图标作为正式源码资源提交，并支持源码运行、安装运行和单文件 EXE 三种资源路径。
- 仓库改为公开；保留已发布的 v1.0.0，不重写历史标签。
- 打印逻辑与现有紧凑界面布局保持不变。

## 1.0.0 — 2026-09-18

- 首个整理后的 Nijika Print 正式版本。
- 采用本地 WebView2 桌面窗口，保持独立 APP 形态。
- 文件工具栏移入左栏；打印设置标题与短打印机选择框同行，其余参数采用两行紧凑控件。
- 删除欢迎横幅、帮助按钮及装饰性文案；缩小文件行高，移除队列的路径副行。
- 打印引擎整合到当前项目，不再动态加载旧版源码，也不再依赖 Tk / CustomTkinter。
- 保留原有打印分发、Office/WPS 自动化、PDF 拼版、队列去重、重排、任务快照和取消逻辑。
- 清理旧版源码、旧 EXE、启动脚本、图标文件、测试样例、日志和旧构建产物。
- 提供单文件 Windows x64 Release EXE 和 SHA-256 校验文件；二进制文件不进入源码提交。

验证覆盖队列操作、参数校验、取消、任务快照、界面结构、独立引擎加载及 PDF 拼版。未执行实体打印机出纸测试。


